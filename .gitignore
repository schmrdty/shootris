/.next
/node_modules

# local env files (contain keys) — .env.local.example is the committed template
.env
.env.local
.env*.local
*.env*
.en*
.DS_Store
next.log
.vercel

output.log
error.log

core

# Foundry build artifacts (contracts/ is staged source only)
contracts/out
contracts/cache
contracts/lib
contracts/broadcast
