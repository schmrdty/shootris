/.next
/node_modules

# local env files (contain keys) — .env.local.example is the committed template
.env
.env.local
.env*.local
*.env*
.en*
.DS_Store
next.log
.vercel

output.log
error.log

core

# Foundry build artifacts (contracts/ is staged source only)
contracts/out
contracts/cache
contracts/lib
contracts/broadcast

# Art-direction prompts stay local (kept out of the public repo)
design/tetromino-prompts.md
design/stage-prompts.md

# Design deliverables stay local: NFT whole-piece art, mint-only card art,
# handoff notes and working files. Only the 256px tiles in public/skins ship.
art/
cards/
.unknown/
.files/
cX/

# TypeScript incremental build cache
*.tsbuildinfo
